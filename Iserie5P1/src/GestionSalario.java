public class GestionSalario {
    public double aumentarSalario(double salario, double aumento) {
        return salario + aumento;
    }

    public double calcularImpuesto(double salario) {
        return salario * 0.15; // Ejemplo de cálculo de impuesto
    }
}
